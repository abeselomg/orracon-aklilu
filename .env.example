# Local Docker: postgresql://orracon:orracon@localhost:55432/tickets?schema=public
# Neon (Vercel): use the pooled connection string from the Neon dashboard.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/tickets?sslmode=require"
